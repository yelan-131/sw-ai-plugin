<UserControl x:Class="SwComAddin.Views.PartPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Border BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#F5F5F5" Padding="6,3">
                <TextBlock x:Name="PreviewTitle" Text="数模预览" FontSize="10" Foreground="#888888"/>
            </Border>
            <Viewport3D x:Name="PreviewViewport" Grid.Row="1" Height="180"
                        ClipToBounds="True" />
            <TextBlock x:Name="NoPreviewText" Grid.Row="1"
                       Text="暂无预览图，待后续更新"
                       FontSize="11" Foreground="#999999"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Visible"/>
        </Grid>
    </Border>
</UserControl>
