# 部署与运维指南

> 前置条件、构建、注册、运行、配置、日志、故障排查

---

## 目录

- [前置条件](#前置条件)
- [构建](#构建)
  - [1. 构建 COM 插件](#1-构建-com-插件)
  - [2. 构建原生垫片（可选）](#2-构建原生垫片可选)
  - [3. 安装 Python 依赖](#3-安装-python-依赖)
- [注册与卸载](#注册与卸载)
  - [方式 A：注册脚本（推荐）](#方式-a注册脚本推荐)
  - [方式 B：手动注册](#方式-b手动注册)
  - [卸载](#卸载)
- [运行](#运行)
  - [方式 1：COM 插件（完整体验）](#方式-1com-插件完整体验)
  - [方式 2：启动脚本](#方式-2启动脚本)
  - [方式 3：VBA 宏启动（备用）](#方式-3vba-宏启动备用)
- [配置](#配置)
  - [API Key](#api-key)
  - [后端地址](#后端地址)
- [日志](#日志)
- [故障排查](#故障排查)
  - [插件不显示在 TaskPane](#插件不显示在-taskpane)
  - [键盘无法输入](#键盘无法输入)
  - [AI 助手连接失败](#ai-助手连接失败)
  - [标准件无法插入](#标准件无法插入)
- [项目文件结构](#项目文件结构)
- [相关文档](#相关文档)

---

## 前置条件

| 依赖 | 版本 | 说明 |
|------|------|------|
| SolidWorks | 2020+ | 已安装并激活 |
| .NET Framework | 4.8 | Windows 内置 |
| .NET SDK | 8.0+ | 用于构建 SwComAddin |
| Python | 3.8+ | 运行 AI 后端 |
| MSVC 编译器 | VS 2019+ | 构建原生垫片（可选） |
| Anthropic API Key | - | AI 智能助手功能必需 |

---

## 构建

### 1. 构建 COM 插件

```bash
cd SwComAddin
dotnet build -c Debug
```

输出：`bin\Debug\net48\SwComAddin.dll`

**注意**：`SwComAddin.csproj` 中 SolidWorks 互操作 DLL 路径硬编码为：
```
D:\Program Files\SOLIDWORKS Corp\SOLIDWORKS\SolidWorks.Interop.sldworks.dll
```
如果你的 SW 安装路径不同，需修改 csproj 中的 `HintPath`。

### 2. 构建原生垫片（可选）

```bash
cd SwNativeShim
build.bat
```

输出：`SwNativeShim.dll`

需要 MSVC 编译环境（Developer Command Prompt）。此步仅在需要原生 COM 垫片降级时必要。

### 3. 安装 Python 依赖

```bash
cd SwAiBackend
pip install -r requirements.txt
```

依赖：`fastapi>=0.115.0`, `uvicorn>=0.34.0`, `anthropic>=0.89.0`

---

## 注册与卸载

### 方式 A：注册脚本（推荐）

以管理员身份运行：

```bash
cd SwComAddin
register.bat
```

脚本执行步骤：
1. `dotnet build -c Debug` — 构建
2. `regsvr32 /u SwNativeShim.dll` — 卸载旧原生垫片
3. `RegAsm /unregister SwComAddin.dll` — 清理旧注册
4. `RegAsm /codebase /tlb SwComAddin.dll` — 注册托管 DLL

### 方式 B：手动注册

```bash
# 构建
cd SwComAddin
dotnet build -c Debug

# 注册（需管理员权限）
%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\RegAsm.exe ^
    bin\Debug\net48\SwComAddin.dll /codebase /tlb
```

### 卸载

```bash
# 卸载 COM 插件
%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\RegAsm.exe ^
    bin\Debug\net48\SwComAddin.dll /unregister /tlb

# 或使用卸载脚本
cd SwComAddin
unregister.bat
```

---

## 运行

### 方式 1：COM 插件（完整体验）

1. 启动 AI 后端：
   ```bash
   cd SwAiBackend
   python app.py
   ```
   后端启动在 `http://localhost:8765`

2. 启动 SolidWorks
3. 菜单：工具 → 插件 → 勾选 "SolidWorks AI Plugin"
4. 右侧 TaskPane 出现插件面板

### 方式 2：启动脚本

```bash
start.bat
```

脚本会依次启动后端和独立 WPF 前端。注意此方式运行的是 `SwAiPlugin`（旧版独立前端），不嵌入 SolidWorks。

### 方式 3：VBA 宏启动（备用）

1. SolidWorks 中打开宏编辑器
2. 加载 `SwMacroPlugin/LaunchPlugin.vb`
3. 运行 `Main` 宏

---

## 配置

### API Key

在插件的"系统设置"页签中：
1. 输入 Anthropic API Key
2. 点击"保存"
3. 点击"验证"确认连接

或设置环境变量：
```bash
set ANTHROPIC_API_KEY=sk-ant-api03-xxxxx
```

### 后端地址

默认 `http://localhost:8765`，可在设置页签修改并保存。

---

## 日志

日志文件位于桌面 `SwAddin.log`，记录：
- COM 连接/断开事件
- TaskPane 创建过程
- 零件库加载状态
- 3D 预览渲染信息

---

## 故障排查

### 插件不显示在 TaskPane

1. 确认 `register.bat` 已以管理员权限运行成功
2. 检查注册表项：`HKLM\SOFTWARE\SolidWorks\Addins\{B3E7D8A1-4F2C-4A91-B5D6-E8F0A1C2D3E4}`
3. 查看 `SwAddin.log` 是否有 "ConnectToSW called" 记录
4. 如果日志显示 "native shim" 而非 "managed" → RegAsm 注册未生效

### 键盘无法输入

此问题仅在 TaskPane 内的 WPF 控件中出现。`SwTaskPaneControl` 的键盘钩子应已修复此问题。如果仍有问题：
- 确认焦点在输入框内（点击输入框）
- 查看 `SwAddin.log` 中是否有钩子安装相关错误

### AI 助手连接失败

1. 确认后端已启动：`curl http://localhost:8765/api/health`
2. 检查 API Key 是否已配置
3. 查看后端控制台输出的错误信息

### 标准件无法插入

`InsertBtn_Click` 使用 `OpenDoc6` 打开 `{id}.SLDPRT` 文件，需要零件文件实际存在于 SW 搜索路径中。当前版本中大部分零件的 `file_path` 为空，因此插入操作会失败。需要：
- 生成实际的 `.sldprt` 文件到正确路径
- 或修改插入逻辑使用参数化建模生成零件

---

## 项目文件结构

```
sw-ai-plugin/
├── start.bat                    # 一键启动脚本
├── SwComAddin/                  # COM 插件（主项目）
│   ├── SwAddin.cs               # COM 入口 + 注册
│   ├── SwTaskPaneControl.cs     # WPF 宿主 + 键盘修复
│   ├── register.bat             # 注册脚本
│   ├── unregister.bat           # 卸载脚本
│   ├── Services/
│   │   ├── SwConnector.cs       # SW API 桥接
│   │   └── ParametricBuilder.cs # 参数化建模
│   ├── Views/
│   │   ├── MainTaskPaneView.xaml(.cs)  # 主界面 5 页签
│   │   └── PartPreviewView.xaml(.cs)   # 3D 预览
│   ├── Models/
│   │   ├── StandardPart.cs      # 标准件数据模型
│   │   ├── CustomLibrary.cs     # 自定义库模型
│   │   ├── ModelingFeatures.cs  # 特征建模抽象
│   │   └── PartFeatureTemplates.cs # 14 种标准件模板
│   └── Data/
│       ├── standard_parts.json  # 标准件库（40+ 种）
│       └── custom_library.json  # 用户自定义库
├── SwAiBackend/                 # Python AI 后端
│   ├── app.py                   # FastAPI 服务
│   ├── sw_command_parser.py     # Claude 命令解析
│   ├── config_manager.py        # 配置管理
│   ├── requirements.txt         # Python 依赖
│   └── prompts/
│       └── system_prompt.txt    # AI 系统提示词
├── SwNativeShim/                # C++ 原生 COM 垫片
│   ├── SwNativeShim.cpp        # 安全降级实现
│   └── build.bat               # 构建脚本
├── SwMacroPlugin/               # VBA 宏启动器
│   └── LaunchPlugin.vb         # 备用启动方式
├── SwAiPlugin/                  # 独立 WPF 前端（旧版）
│   ├── MainWindow.xaml(.cs)    # 独立窗口主界面
│   ├── Services/               # 服务层
│   └── Views/                  # 视图层
├── DumpApi/                     # API 探查工具（开发用）
└── docs/                        # 项目文档
    ├── README.md                # 文档中心（本文件）
    ├── 架构总览.md              # 系统架构 + 技术栈 + 数据流
    ├── 模块详解.md              # 6 个子项目逐一拆解
    ├── 接口与数据协议.md        # HTTP API + 命令协议 + 数据结构
    └── 部署与运维指南.md        # 构建 + 注册 + 运行 + 故障排查
```

---

## 相关文档

| 想了解... | 请看 |
|-----------|------|
| 系统整体架构和数据流 | [架构总览](架构总览.md) |
| 每个模块的详细类和方法 | [模块详解](模块详解.md) |
| HTTP API 参数和返回值 | [接口与数据协议](接口与数据协议.md) |
