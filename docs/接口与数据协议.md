# 接口与数据协议

> 后端 HTTP API、AI 命令协议、标准件数据结构、特征建模类型、配置文件格式

---

## 目录

- [接口与数据协议](#接口与数据协议)
  - [目录](#目录)
  - [后端 HTTP API](#后端-http-api)
    - [GET /api/health — 健康检查](#get-apihealth--健康检查)
    - [POST /api/chat — 自然语言建模](#post-apichat--自然语言建模)
    - [GET /api/config — 查询配置](#get-apiconfig--查询配置)
    - [POST /api/config — 保存配置](#post-apiconfig--保存配置)
  - [AI 命令协议（19 种命令）](#ai-命令协议19-种命令)
    - [基础操作（4 种）](#基础操作4-种)
    - [草图操作（7 种）](#草图操作7-种)
    - [特征操作（5 种）](#特征操作5-种)
    - [装配体操作（2 种）](#装配体操作2-种)
  - [标准件数据结构](#标准件数据结构)
    - [JSON 格式示例](#json-格式示例)
    - [字段说明](#字段说明)
    - [TreeView 数据层级](#treeview-数据层级)
  - [特征建模数据结构](#特征建模数据结构)
    - [Profile（截面）](#profile截面)
    - [ModelFeature（特征）](#modelfeature特征)
    - [FeatureList（特征脚本）](#featurelist特征脚本)
  - [配置文件](#配置文件)
    - [用户配置 (user\_config.json)](#用户配置-user_configjson)
    - [插件元数据 (plugin\_meta.json)](#插件元数据-plugin_metajson)
    - [旧版迁移](#旧版迁移)
    - [自定义库 (custom\_library.json)](#自定义库-custom_libraryjson)
    - [AI 后端配置 (config.json)](#ai-后端配置-configjson)
    - [发布清单 (manifest.json)](#发布清单-manifestjson)
    - [更新错误码](#更新错误码)
  - [相关文档](#相关文档)

---

## 后端 HTTP API

基础地址：`http://localhost:8765`

### GET /api/health — 健康检查

**响应**：
```json
{ "status": "ok" }
```

### POST /api/chat — 自然语言建模

发送中文自然语言，获取建模命令。

**请求**：
```json
{ "message": "创建一个M10×50的六角螺栓" }
```

**成功响应**：
```json
{
  "reply": "正在创建M10×50六角螺栓",
  "commands": [
    { "action": "new_part" },
    { "action": "select_plane", "plane": "top" },
    { "action": "sketch_start" },
    { "action": "sketch_circle", "center_x": 0, "center_y": 0, "radius": 5.0 },
    { "action": "sketch_end" },
    { "action": "extrude", "depth": 50.0 }
  ]
}
```

**错误响应**（未配置 API Key）：
```json
{ "reply": "错误：未设置ANTHROPIC_API_KEY环境变量", "commands": [] }
```

### GET /api/config — 查询配置

获取 API Key 配置状态（脱敏显示）。

**响应**：
```json
{
  "anthropic_api_key_set": true,
  "masked_key": "sk-ant-a...x1y2"
}
```

### POST /api/config — 保存配置

保存 API Key 到后端 config.json。

**请求**：
```json
{ "anthropic_api_key": "sk-ant-api03-xxxxx" }
```

**响应**：
```json
{ "status": "ok", "message": "配置已保存" }
```

---

## AI 命令协议（19 种命令）

Claude 返回的 `commands` 数组中每个元素格式为：

```json
{ "action": "<命令名>", "<参数1>": <值1>, "<参数2>": <值2> }
```

### 基础操作（4 种）

| action | 参数 | 说明 |
|--------|------|------|
| `new_part` | 无 | 新建零件文档 |
| `new_assembly` | 无 | 新建装配体文档 |
| `open_file` | `filename: string` | 打开文件 |
| `save_file` | `filename?: string` | 保存文件（可选指定名称） |

### 草图操作（7 种）

| action | 参数 | 说明 |
|--------|------|------|
| `select_plane` | `plane: "top"/"front"/"right"` | 选择草图基准面 |
| `sketch_start` | 无 | 进入草图模式 |
| `sketch_end` | 无 | 退出草图模式 |
| `sketch_circle` | `center_x, center_y, radius: double` | 绘制圆 |
| `sketch_rectangle` | `x1, y1, x2, y2: double` | 绘制矩形（两个对角点） |
| `sketch_line` | `x1, y1, x2, y2: double` | 绘制直线 |
| `sketch_arc` | `center_x, center_y, radius, start_angle, end_angle: double` | 绘制圆弧 |

### 特征操作（5 种）

| action | 参数 | 说明 |
|--------|------|------|
| `extrude` | `depth: double`, `direction?: "forward"/"backward"` | 拉伸凸台 |
| `revolve` | `axis: "x"/"y"/"z"`, `angle?: double` | 旋转体（默认 360°） |
| `cut_extrude` | `depth: double` | 拉伸切除 |
| `add_fillet` | `radius: double`, `edges: int[]` | 添加圆角 |
| `add_chamfer` | `distance: double`, `edges: int[]` | 添加倒角 |

### 装配体操作（2 种）

| action | 参数 | 说明 |
|--------|------|------|
| `insert_component` | `file_path: string`, `x, y, z: double` | 插入零部件到指定位置 |
| `add_mate` | `type: string`, `selection1, selection2: string` | 添加配合关系 |

**单位约定**：尺寸 mm，角度 degree。

---

## 标准件数据结构

`Data/standard_parts.json` 格式：

### JSON 格式示例

```json
{
  "categories": [
    {
      "name": "紧固件",
      "icon": "bolt",
      "subcategories": [
        {
          "name": "六角头螺栓",
          "parts": [
            {
              "id": "hex_bolt_m6",
              "name": "六角头螺栓 M6",
              "standard": "GB/T 5782",
              "geometric": true,
              "feature_template": "hex_bolt",
              "specs": { "d": 6, "s": 10, "k": 4.0, "r": 0.25 },
              "performance": {},
              "file_path": "",
              "description": "标准六角头螺栓，M6螺纹"
            }
          ]
        }
      ],
      "parts": []
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | 是 | 唯一标识符，也用作插入时的文件名 |
| `name` | string | 是 | 界面显示名称 |
| `standard` | string | 是 | 国标号或标准名 |
| `geometric` | bool | 是 | 是否有几何模型（传感器等为 false） |
| `feature_template` | string? | 否 | 对应 `PartFeatureTemplates` 的方法名 |
| `specs` | object | 是 | 尺寸参数键值对 |
| `performance` | object | 否 | 性能参数（载荷/转速等） |
| `file_path` | string | 否 | 零件文件路径（当前为空，待生成） |
| `description` | string | 是 | 中文描述 |

### TreeView 数据层级

```
Category (1级: 紧固件/轴承/电机/气缸/传感器/导轨)
  └── SubCategory (2级: 六角头螺栓/螺母/垫圈/...)
        └── StandardPart (3级: 具体规格零件)
```

如果 Category 只有 `parts` 没有 `subcategories`，加载时会自动包装为一个同名 SubCategory。

---

## 特征建模数据结构

`ModelingFeatures.cs` 定义的平台无关特征模型。

### Profile（截面）

```csharp
abstract class Profile { }
class CircleProfile : Profile { double Diameter; }
class RectangleProfile : Profile { double Width, Height; }
class PolygonProfile : Profile { int Sides; double Diameter; }
```

### ModelFeature（特征）

```csharp
abstract class ModelFeature { }

class ExtrudeFeature : ModelFeature {
    Profile Profile; double Depth; ExtrudeDirection Direction;
    double OffsetX, OffsetY; string Plane, MergeRule;
}

class ExtrudeCutFeature : ModelFeature {
    Profile Profile; double Depth; double OffsetX, OffsetY; string Plane;
}

class RevolveFeature : ModelFeature {
    Profile Profile; double Angle; string Plane;
}

class ChamferFeature : ModelFeature { double Size; string Edge; }
class FilletFeature : ModelFeature { double Radius; string Edge; }
class HoleFeature : ModelFeature {
    double Diameter, Depth, PositionX, PositionY; string Plane;
}
```

### FeatureList（特征脚本）

```csharp
class FeatureList {
    List<ModelFeature> Features;
    void AddExtrude(Profile, depth, plane, merge, offsetX, offsetY);
    void AddExtrudeCut(Profile, depth, plane, offsetX, offsetY);
    void AddRevolve(Profile, angle, plane);
    void AddChamfer(size, edge);
    void AddFillet(radius, edge);
    void AddHole(diameter, depth, posX, posY, plane);
}
```

---

## 配置文件

### 用户配置 (user_config.json)

位于 SwComAddin 输出目录。用户私有运行时设置，更新流程 preserve（不覆盖），不随发布包发布。

```json
{
  "backend_url": "http://localhost:8765",
  "model_library_path": "",
  "update_source": "auto",
  "skipped_versions": [],
  "defer_until_utc": null,
  "check_interval_hours": 4,
  "receive_prerelease": false,
  "mirror_url": null
}
```

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| backend_url | string | "http://localhost:8765" | AI 后端地址 |
| model_library_path | string | "" | 用户模型库路径 |
| update_source | string | "auto" | 更新源：auto / gitee / github / mirror |
| skipped_versions | string[] | [] | 用户主动跳过的版本列表 |
| defer_until_utc | string? | null | "稍后提醒"到期时间（ISO8601 UTC） |
| check_interval_hours | int | 4 | 自动检查更新周期（小时） |
| receive_prerelease | bool | false | 是否接收 Beta 通道版本 |
| mirror_url | string? | null | 企业镜像 URL，非空时强制使用此源 |

### 插件元数据 (plugin_meta.json)

位于 SwComAddin 输出目录。随发布包覆盖，由 Updater 在更新成功后写入，主插件运行中只读。

```json
{
  "schema": "1.0",
  "version": "0.1.2",
  "channel": "stable",
  "build_date": "2026-05-14T17:52:32Z",
  "update_repo": "yelan-131/sw-ai-plugin",
  "gitee_repo": "yelan1387/sw-ai-plugin"
}
```

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| schema | string | "1.0" | 数据格式版本 |
| version | string | "0.1.1" | 当前安装版本（SemVer） |
| channel | string | "stable" | 发布通道（stable / beta / dev） |
| build_date | string | "" | 构建时间 |
| update_repo | string | "yelan-131/sw-ai-plugin" | GitHub 更新仓库 |
| gitee_repo | string | "yelan1387/sw-ai-plugin" | Gitee 更新仓库 |

### 旧版迁移

v0.1.1 及更早版本使用单一 `plugin_config.json`。插件启动时 `PluginConfigService` 自动检测并迁移：

1. 读取旧 `plugin_config.json` 内容
2. 拆分为 `user_config.json`（用户设置）和 `plugin_meta.json`（版本元数据）
3. 旧文件重命名为 `plugin_config.json.legacy.bak`

迁移幂等：如果新文件已存在则跳过。迁移失败不致命，保留旧文件供下次启动重试。

### 自定义库 (custom_library.json)

位于 SwComAddin/Data/ 目录，用户自建的零件和模板：

```json
{
  "Parts": [
    {
      "Id": "abc12345",
      "Name": "自定义零件",
      "Category": "分类",
      "Standard": "",
      "Specs": {},
      "Notes": ""
    }
  ],
  "Templates": [
    {
      "Id": "def67890",
      "Name": "自定义模板",
      "Parameters": [
        { "Key": "d", "Label": "直径", "DefaultValue": "10", "Unit": "mm" }
      ]
    }
  ]
}
```

### AI 后端配置 (config.json)

位于 SwAiBackend 目录，存储 Anthropic API Key：

```json
{ "anthropic_api_key": "sk-ant-api03-xxxxx" }
```

也可通过环境变量 `ANTHROPIC_API_KEY` 设置，优先级高于配置文件。

### 发布清单 (manifest.json)

随 Release 上传到 GitHub/Gitee 的元数据文件。客户端检查更新时只需下载 manifest.json（< 5KB），即可判定是否升级、是否兼容，无需下载完整 ZIP。

```json
{
  "schema": "1.0",
  "version": "0.1.2",
  "channel": "stable",
  "released_at": "2026-05-14T17:52:33Z",
  "min_plugin_version": "0.1.0",
  "min_sw_version": 2020,
  "min_dotnet": "4.8",
  "force_update": false,
  "package": {
    "name": "SwAiPlugin_v0.1.2.zip",
    "size": 1402330,
    "sha256": "5cc5ceaa9662dce83687b1aac7cb739d0fa1a6d416c76ee87bcf8d8ba1cdb3c0",
    "primary_url": "https://github.com/yelan-131/sw-ai-plugin/releases/download/v0.1.2/SwAiPlugin_v0.1.2.zip",
    "mirrors": [
      "https://gitee.com/yelan1387/sw-ai-plugin/releases/download/v0.1.2/SwAiPlugin_v0.1.2.zip"
    ]
  },
  "files": [
    { "path": "SwComAddin.dll", "sha256": "6d765ba...", "action": "replace" }
  ],
  "preserve": ["user_config.json", "Data/custom_library.json"],
  "backend": { "version": "0.1.2", "requirements_changed": false },
  "release_notes_url": "https://github.com/yelan-131/sw-ai-plugin/releases/tag/v0.1.2",
  "release_notes_summary": "v0.1.2 - Builder 模式重构 + 完整更新系统",
  "release_notes": [
    {
      "section": "参数化建模",
      "items": ["重构为 Builder 模式，新增 9 种零件构建器"]
    }
  ]
}
```

**顶层字段：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| schema | string | 是 | 数据格式版本 "1.0" |
| version | string | 是 | 发布版本号（SemVer） |
| channel | string | 是 | stable / beta / dev |
| released_at | string | 是 | 发布时间（ISO8601） |
| min_plugin_version | string? | 否 | 最低可增量升级版本，低于此版本需重装 |
| min_sw_version | int? | 否 | 最低 SolidWorks 主版本号（如 2020） |
| min_dotnet | string? | 否 | 所需 .NET Framework 版本（如 "4.8"） |
| force_update | bool | 否 | 关键安全更新，UI 不提供"稍后"选项 |
| package | object | 是 | 下载包信息（见下表） |
| files | array | 否 | 逐文件清单（见下表） |
| preserve | string[] | 否 | 更新时不覆盖的文件（相对安装目录） |
| backend | object? | 否 | 后端兼容信息（见下表） |
| release_notes_url | string? | 否 | Release 页面链接 |
| release_notes_summary | string? | 否 | 更新日志纯文本摘要（release_notes 为空时 fallback） |
| release_notes | array? | 否 | 结构化更新日志（见下表） |

**package 子对象：**

| 字段 | 类型 | 说明 |
|------|------|------|
| name | string | ZIP 文件名 |
| size | long | 文件大小（字节） |
| sha256 | string | SHA256 校验值（小写十六进制） |
| primary_url | string | 主下载 URL |
| mirrors | string[] | 镜像 URL 列表（fallback） |

**files 数组元素：**

| 字段 | 类型 | 说明 |
|------|------|------|
| path | string | 文件路径（相对安装目录） |
| sha256 | string? | 文件 SHA256 校验值 |
| action | string | replace（覆盖） / delete（新版本中删除） |

**release_notes 数组元素（ReleaseNoteSection）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| section | string | 分区标题（如"参数化建模"） |
| items | string[] | 编号列表项 |

**backend 子对象（BackendInfo）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| version | string | 后端版本号 |
| requirements_changed | bool | Python 依赖是否有变更（变更时提示用户重新 pip install） |

### 更新错误码

由 `UpdateErrorCodes` 静态类定义，用于日志和 UI 展示。

| 阶段 | 错误码 | 说明 |
|------|--------|------|
| 检查 | UPD-CHECK-NETWORK | 无法联系任何更新源 |
| 检查 | UPD-CHECK-PARSE | 版本号解析失败 |
| 检查 | UPD-CHECK-INCOMPATIBLE | 当前版本过低，无法增量升级 |
| 下载 | UPD-DL-TIMEOUT | 下载超时 |
| 下载 | UPD-DL-HTTP | HTTP 请求失败 |
| 下载 | UPD-DL-CANCELLED | 用户取消下载 |
| 校验 | UPD-VERIFY-HASH | SHA256 校验不匹配 |
| 校验 | UPD-VERIFY-SIG | 签名验证失败（预留，Iteration 2 启用） |
| 安装 | UPD-INSTALL-EXTRACT | 解压失败 |
| 安装 | UPD-INSTALL-COPY | 文件复制失败 |
| 安装 | UPD-INSTALL-REGASM | RegAsm COM 注册失败 |
| 安装 | UPD-INSTALL-SW-BUSY | SolidWorks 有未保存文档 |
| 回滚 | UPD-ROLLBACK-OK | 回滚成功 |
| 回滚 | UPD-ROLLBACK-FAIL | 回滚失败 |

---

## 相关文档

| 想了解... | 请看 |
|-----------|------|
| 系统整体架构和数据流 | [架构总览](架构总览.md) |
| 每个模块的详细类和方法 | [模块详解](模块详解.md) |
| 如何构建、注册、运行 | [部署与运维指南](部署与运维指南.md) |
