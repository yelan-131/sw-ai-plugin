# 运行方式

## 运行

### 方式 1：COM 插件（完整体验）

1. 启动 AI 后端：
   ```bash
   cd SwAiBackend
   python app.py
   ```
   后端启动在 `http://localhost:8765`

2. 启动 SolidWorks
3. 菜单：工具 → 插件 → 勾选 "SolidWorks AI Plugin"
4. 右侧 TaskPane 出现插件面板

### 方式 2：启动脚本

```bash
start.bat
```

脚本会依次启动后端和独立 WPF 前端。注意此方式运行的是 `SwAiPlugin`（旧版独立前端），不嵌入 SolidWorks。

### 方式 3：VBA 宏启动（备用）

1. SolidWorks 中打开宏编辑器
2. 加载 `SwMacroPlugin/LaunchPlugin.vb`
3. 运行 `Main` 宏

---

## 相关文档

| 想了解... | 请看 |
|-----------|------|
| 构建与注册 | [构建与注册](构建与注册.md) |
| 运行方式 | [运行](运行.md) |
| 配置 | [配置](配置.md) |
| 发布打包 | [发布打包](发布打包.md) |
| 日志 | [日志](日志.md) |
| 故障排查 | [故障排查](故障排查.md) |
